var NISLFuzzingFunc = function(a) {
    var c = this.angularVelocity * a;
    return a instanceof c.Matrix ? a.matrix = a.matrix || a.createMatrix(), a.matrix = this.matrix, 
    a.matrixWorldNeedsUpdate = !0, a.matrixWorld = this.matrix, a;
}
;
var NISLParameter0 = true;
var NISLCallingResult = NISLFuzzingFunc(NISLParameter0);
print(NISLCallingResult);
