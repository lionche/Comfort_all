var NISLFuzzingFunc = function() {
    var e = this.elements;
    return e[0] = e[0] * Math.cos(e[1] * (0 < e[0]), 0 < e[1] ? e[1] : 0);
}
;
var NISLCallingResult = NISLFuzzingFunc();
print(NISLCallingResult);
